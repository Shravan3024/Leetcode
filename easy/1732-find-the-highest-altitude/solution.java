
        for(int i = 0; i <= gain.length-1; i++){
            current += i;
        }
            max = Math.max(max, current);
        return max;
        int max = 0;
        int current = 0;
    public int largestAltitude(int[] gain) {
    }
class Solution {
